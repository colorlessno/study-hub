# 実行案

## 実行案

固定fixtureを読み、各ロールの成果物を選択したモードで順番に作成して `runs/<run-id>/` に保存し、確認スクリプトで構造を検査する。

## 変更対象

教材内の `runs/<run-id>/` 配下に実行単位の成果物を保存する。

## 実行しない操作

- 外部送信
- 未承認のファイル削除
- OS設定変更
- 実秘密情報の参照

## 承認待ち

通常ケースでは承認待ちはない。ファイル変更、ファイル削除、コマンド実行を含む依頼では、安全確認で承認待ちにする。
