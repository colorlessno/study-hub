# レビュー報告

## 指摘事項

- ローカルLLM実通信と模擬実行を明示的に選び、結果を混同しない必要がある。
- AIが危険操作を実行しない境界を明記する必要がある。

## 重大度

- 実通信と模擬実行の区別不足: 中
- 安全境界の不足: 高

## 対応案

- `mock`と`local_llm`の選択を明示し、接続失敗時は自動切替せず停止理由を保存する。
- ファイル変更、コマンド実行、外部送信を承認待ちにする。

## 残リスク

ローカルLLMの出力品質はモデルに依存するため、確認スクリプトは構造と安全境界の検査に限定する。
