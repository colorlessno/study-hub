# 時間切れの確認

この教材の遅い応答は、要求を受け取ってから2秒後に状態番号200を返します。StudyHub側の待機上限は1秒なので、応答を受け取る前に処理を打ち切ります。

## 画面で確認する項目

- `ok`が`false`になっている
- `output.error`が`request_timeout`になっている
- `timeoutMilliseconds`が1000になっている
- サーバーの処理時間より呼び出し側の上限が短い

時間切れは失敗をなくす設定ではありません。待ち続ける時間を制限し、利用者への案内、再試行、処理中止などの次の判断へ進むための設定です。
