# Issue: web04でDOM操作の基本を確認できるサンプルを作る

これはIssueの記入例です。実際の作業では、作業開始前に対象と完了条件を確定します。

## 背景

HTML、CSS、JavaScriptの役割を分けたうえで、ボタン操作によるDOM変更を練習したい。

## 目的

`addEventListener`を使って、画面上のテキストを変更できるサンプルを作る。

## 対象範囲

- [x] `index.html`を作る
- [x] `styles.css`を作る
- [x] `script.js`を作る
- [x] READMEに確認手順を書く

## 完了条件

- [x] ボタン押下で表示テキストが変わる
- [x] リセットで初期表示に戻る
- [x] HTMLの`onclick`属性を使っていない

## 対象外

フォーム送信、API通信、DB保存は扱わない。

## 確認方法

- 操作: ページを開き、「表示を変える」と「リセット」を押す。
- 確認対象: 表示テキストとブラウザ開発者ツールのコンソール。
- 期待する結果: 表示が切り替わり、リセット後に初期表示へ戻り、JavaScriptエラーが出ない。

## 補足

秘密情報や個人情報を扱わない静的Web教材を対象とする。
