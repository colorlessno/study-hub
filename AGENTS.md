# リポジトリ全体の実装ルール

## 同時並列処理の禁止

- この禁止に例外を設けない。
- このリポジトリが提供するすべての機能、コード、テスト、スクリプト、設定、文書では、複数の処理を同時または並列に実行してはならない。
- 依存ライブラリや実行環境の内部機能を理由に、リポジトリの機能へ同時並列処理を導入してはならない。
- 複数のモデル、外部API、ファイル、テーマ、プロセス、画面用データを扱う場合は、対象を一つずつ順番に処理する。
- `asyncio.gather`、`asyncio.create_task`、`TaskGroup`、`Promise.all`、`Promise.allSettled`など、複数処理を同時に開始する構造を使用してはならない。
- 複数のモデル名や接続先が設定されていても、同時接続・同時送信・並列比較を行ってはならない。
- 要件定義、設計書、README、テストに同時並列処理の記載を追加してはならない。既存の記載は順次処理へ修正する。
